[agents]
max_threads = 6
max_depth = 1

[mcp_servers.konnect]
# Install the pinned Konnect release binary first with `mise install`.
command = "mise"
args = ["exec", "-C", ".", "--", "konnect", "--client", "codex", "--config", "konnect/config.toml"]
